/// This file is generated by kopgen. Do not edit manually. If you need to make adjustments add it to .openapi-generator-ignore file.
use clap::{Parser, Subcommand};

/// Command-line interface for the Kubernetes Operator Codegen tool.
#[derive(Parser)]
#[command(
    name = "{{ project_name }}",
    version = "{{ version }}",
    author = "{{ author }}",
    arg_required_else_help = true
)]
pub struct Cli {
    /// Sets the level of verbosity
    #[arg(
        long,
        default_value = "info",
        env = "RUST_LOG",
        help = "Sets the level of verbosity"
    )]
    pub verbosity: String,

    /// The command to execute.
    #[command(subcommand)]
    pub command: Option<Commands>,
}

/// Available commands for the CLI.
#[derive(Subcommand)]
pub enum Commands {
    /// Runs the Kubernetes operator.
    #[command(about = "Run the Kubernetes operator")]
    Run {
        /// Option to install CRDs
        #[arg(
            long,
            env = "INSTALL_CRDS",
            help = "Install CRDs before running the operator"
        )]
        install_crds: bool,
    },
    /// Displays the CLI version.
    #[command(about = "Show the CLI version")]
    Version,
}
